def using_push(inArray, inString)
  inArray.push(inString)
end

def using_unshift(inArray, inString)
  inArray.unshift(inString)
end

def using_pop(inArray)
  inArray.pop()
end

def pop_with_args(inArray, popCount)
  inArray.pop(popCount)
end

def using_shift(inArray)
  inArray.shift()
end

def shift_with_args(inArray, shiftCount)
  inArray.shift(shiftCount)
end

def using_concat(array1, array2)
  array